---
import type { Studies } from '@types/Studies'

interface Props extends Studies {}

const { course, institution, location, date_start, date_end, description } =
  Astro.props
---

<article class='grid gap-4'>
  <header class='relative'>
    <span class='text-primary text-sm'>
      {date_start} - {date_end}
    </span>
    <div
      class='size-3 bg-base-200 border-[1px] border-base-300 rounded-full absolute -left-[23px] top-[7px]'
    >
    </div>
  </header>

  <main class='grid gap-4'>
    <div>
      <h3 class='text-xl font-semibold'>{course}</h3>
      <span class='text-sm text-primary'>
        {institution}, {location}
      </span>
    </div>
    <p class='text-sm'>{description}</p>
  </main>
</article>
